<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

	<!-- session-factory internally do connection activities -->
	<session-factory>
		<property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
		<property name="connection.url">jdbc:mysql://localhost:3306/mobile</property>
		<property name="connection.username">root</property>
		<property name="connection.password">Akash@006</property>
		<!-- default it is set true, so need to change it to true -->
		<property name="hibernate.show_sql">true</property>
		<!-- this property formats the sql query written by the hibernate -->
		<property name="hibernate.format_sql">true</property>
	</session-factory>

</hibernate-configuration>